// Import necessary modules and components (if applicable)
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Variable declarations
const totalSaleAmount = 0;
const totalSoldItems = 0;
const totalNotSoldItems = 0;
const barChartData = [];
const pieChartData = [];

// Render the main component (App) to the root DOM element
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
